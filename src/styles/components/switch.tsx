import styled from "styled-components";
import Switch from 'react-switch';

export const SW = styled(Switch).attrs({
  height: 20,
  width: 50
})`
  .switch{
    display: flex;
    justify-content: center;
  }
`;